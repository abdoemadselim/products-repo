// import * as zod from "zod";

// const ProductSchema = zod.object({
//     name: zod.string().trim().min(10).max(100),
//     category:
// })


// export type ProductType = zod.infer<typeof ProductSchema>;
